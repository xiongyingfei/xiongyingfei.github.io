---
name: update-course-website
description: 课后更新本项目课程网站的课件、作业和截止时间。
---

# 课后更新网站

2026 秋季软件分析：数据在 `_data/SA_new/_2026.yaml`，页面在 `SA_new/2026/`。

1. 将新课件放入 `slides/`，读取标题和作业题目。
2. 更新 YAML 的 `lectures`：上课日期、课件标题及文件名、完整作业题目、截止时间。截止时间遵循项目根目录 `AGENTS.md` 的默认规则及延期例外；用户本次明确指定的时间优先。每道题一个 `text`；提醒、排除清单和“无作业”等不需编号的说明项也用 `text`，并加 `note: true`，保持它们与题目的原有顺序。不要把教学计划 `schedule` 当成已发布课次。
3. 核对本次题目、日期和链接，提交改动；已获发布授权时推送，确认上线后给出页面链接。

页面规则：

- 作业页直接展示完整题目和截止时间，不放幻灯片链接，也不加“已在教学网发布”“参见课件”等说明。`assignments.md` 只保留通用规则和标题，题目由 YAML 渲染。
- 首页通知由 `_includes/announcements.html` 生成，每课一行：`日期 幻灯片标题链接 作业链接 · 截止日期`。不加中括号，无作业则省略作业部分；窄屏自然换行。
